# Generate token: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TRACE_RUNNER_TOKEN=
TRACE_ALLOWED_ORIGINS=http://localhost:3000,https://trace-agentic-qa.globalgupta14.chatgpt.site
HOST=127.0.0.1
PORT=4318
# Optional: full OpenAI-compatible chat/completions endpoint (including local models).
MODEL_API_URL=
MODEL_API_KEY=
MODEL_NAME=
# Optional saved Playwright storage state for a dedicated test account:
STORAGE_STATE_PATH=
# Only enable on a disposable test environment:
ALLOW_TEST_WRITES=false
# Optional installed browser channel, such as chrome:
BROWSER_CHANNEL=
